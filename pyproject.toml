[project]
name = "havs-data-analysis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Mandar Upasani", email = "mandar.upasani@altair.in" }
]
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.7",
    "numpy>=1.24",
    "openpyxl>=3.1",
    "scipy>=1.10",
    "setuptools>=82.0.0",
    "soundfile>=0.12",
]

[build-system]
requires = ["uv_build>=0.10.6,<0.11.0"]
build-backend = "uv_build"
